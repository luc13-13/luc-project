<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lc.product.center.mapper.PricingStrategyParamMapper">

    <resultMap id="BaseResultMap" type="com.lc.product.center.domain.entity.PricingStrategyParamDO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="strategy_code" property="strategyCode"/>
        <result column="param_type" property="paramType"/>
        <result column="range_start" property="rangeStart"/>
        <result column="range_end" property="rangeEnd"/>
        <result column="value" property="value"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="created_by" property="createdBy"/>
        <result column="dt_created" property="dtCreated"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, tenant_id, strategy_code, param_type,
        range_start, range_end, value, sort_order,
        created_by, dt_created
    </sql>

    <select id="selectByStrategyCode" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM product_center.pricing_strategy_param
        WHERE tenant_id = #{tenantId}
        AND strategy_code = #{strategyCode}
        ORDER BY sort_order, range_start
    </select>

    <delete id="deleteByStrategyCode">
        DELETE FROM product_center.pricing_strategy_param
        WHERE tenant_id = #{tenantId}
          AND strategy_code = #{strategyCode}
    </delete>

    <insert id="batchInsert" parameterType="java.util.List">
        INSERT INTO product_center.pricing_strategy_param
        (tenant_id, strategy_code, param_type,
         range_start, range_end, value, sort_order, created_by, dt_created)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.tenantId}, #{item.strategyCode}, #{item.paramType},
             #{item.rangeStart}, #{item.rangeEnd}, #{item.value},
             #{item.sortOrder}, #{item.createdBy}, NOW())
        </foreach>
    </insert>

</mapper>
