spring:
  cloud:
    nacos:
      username: 'luc'
      password: 'luc123456'
      server-addr: localhost:8848
      discovery:
        namespace: com-lc-dev
        # 本地测试时需要用本地地址，而非公网ip
        ip: 127.0.0.1
      config:
        # 当前应用的配置文件按照命名规则从nacos获取：${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
        file-extension: yaml
        namespace: com-lc-dev
#        shared-configs:
          # 其他配置文件可单独添加
#          - data-id: common.yml
#            refresh: true