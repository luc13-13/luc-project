server:
  port: 8889

spring:
  application:
    name: authorization-server
  devtools:
    restart:
      enabled: true
  profiles:
    active: dev
# 日志配置
logging:
  level:
    root: info
    org.springframework.security: trace
    com.lc.auth.server: debug
