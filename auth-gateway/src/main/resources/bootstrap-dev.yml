# 网关地址
gateway:
  endpoint: http://localhost

spring:
  cloud:
    # 暂时使用application.yml中的配置，部署时将配置全部迁移到nacos
    nacos:
      username: 'luc'
      password: 'luc123456'
      server-addr: localhost:8848
      discovery:
        namespace: com-lc-dev
        # 本地测试时需要用本地地址，而非公网ip
        ip: 127.0.0.1
        enabled: true
      config:
        namespace: com-lc-dev
        file-extension: yaml
        ## nacos默认根据${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}规则加载配置文件
        ## 根据其他的配置文件可以在shared-configs中声明data-id
#        shared-configs:
#          - data-id: ${spring.application.name}-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
#            refresh: true
